<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Libris</title>

  <!-- Bootstrap (opcional, já está presente) -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

  <!-- Estilo global opcional -->
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    main {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    /* Evita interferência do Bootstrap */
    .container, .row, .col {
      max-width: none !important;
      width: 100% !important;
      padding: 0 !important;
      margin: 0 !important;
    }

    footer {
      text-align: center;
      color: #aaa;
      font-size: 0.8rem;
      margin-top: 20px;
    }
  </style>
</head>
<body>

  <!-- Exibe a navbar apenas fora das páginas de login e cadastro -->
  <% if (!(typeof isLoginPage !== 'undefined' && isLoginPage) && !(typeof isRegisterPage !== 'undefined' && isRegisterPage)) { %>
    <%- include('partials/navbar') %>
  <% } %>

  <main>
    <%- body %>
  </main>

  <!-- Rodapé (não aparece no cadastro) -->
  <% if (typeof isRegisterPage === 'undefined' || !isRegisterPage) { %>
    <footer>
      <small>&copy; 2025 Libris.</small>
    </footer>
  <% } %>

</body>
</html>
