<div class="container mt-5">
  <h2>Solicitações de Empréstimo Pendentes</h2>
  <table class="table table-bordered mt-3">
    <thead>
      <tr>
        <th>Usuário</th>
        <th>Título</th>
        <th>Número de Registro</th>
        <th>Status</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% emprestimos.forEach(e => { %>
        <tr>
          <td><%= e.usuario_nome %></td>
          <td><%= e.titulo %></td>
          <td><%= e.n_registro %></td>
          <td><span class="badge badge-warning"><%= e.status %></span></td>
          <td>
            <form action="/emprestimos/<%= e.id %>/autorizar" method="POST" style="display:inline;">
              <input type="date" name="data_devolucao" required>
              <button type="submit" class="btn btn-sm btn-success">Autorizar</button>
            </form>
            <form action="/emprestimos/<%= e.id %>/recusar" method="POST" style="display:inline;">
              <button type="submit" class="btn btn-sm btn-danger">Recusar</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>