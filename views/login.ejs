<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Libris | Login</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #151F55, #AD8D8D);
      color: #ffffff;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .login-container {
      background-color: #43496A;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.7);
      width: 100%;
      max-width: 400px;
      text-align: center;
    }

    .form-control {
      background-color: #1a1a1a;
      color: #fff;
      border: 1px solid #555;
    }

    .form-control::placeholder {
      color: #aaa;
    }

    .btn-login {
      background-color: #090274;
      border: none;
    }

    .btn-login:hover {
      background-color:#090274 ;
    }

    .logo-img {
      max-width: 120px;
      margin-bottom: 15px;
    }

    .logo-title {
      font-size: 2rem;
      font-weight: bold;
      margin-bottom: 25px;
      color: #CAC4A8;
      letter-spacing: 2px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <!-- Espaço reservado para o logo -->
    <img src="/public/logoescura.png" alt="Logo Libris" class="logo-img" />

    <div class="logo-title">LIBRIS</div>

    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <form action="/login" method="POST">
      <div class="form-group text-left">
        <label for="username">Usuário</label>
        <input type="text" name="username" id="username" class="form-control" placeholder="Digite seu nome" required>
      </div>
      <div class="form-group text-left">
        <label for="password">Senha</label>
        <input type="password" name="password" id="password" class="form-control" placeholder="Digite sua senha" required>
      </div>
      <button type="submit" class="btn btn-login btn-block">Entrar</button>
    </form>
  </div>
</body>
</html>
